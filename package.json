{
  "name": "aitool-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix server install",
    "build": "npm --prefix server run build",
    "start": "npm --prefix server run start:prod"
  }
}
